extends layout

block content
  h1= message
  h2= error.status

  .row
    .offset-sm-2.col-sm-8
      .jumbotron.text-center.
        Sorry! We couldn't find that page :( #[a(href="/", title="") Return home]
